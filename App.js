 // Get the root HTML element
 const root = document.documentElement;

 // Disable the scrollbar by setting the overflow property to hidden
 root.style.overflow = 'hidden';

 // Enable the scrollbar by setting the overflow property back to its default value
 root.style.overflow = 'auto';
